# Initial structure seed — 2026-08-24

Status: **provisional / source-grounded**

Seeded 5 high-school-relevant discrete molecular structures already present in the organic sibling package:

- methane — PubChem CID 297
- ethene — PubChem CID 6325
- ethyne — PubChem CID 6326
- benzene — PubChem CID 241
- ethanol — PubChem CID 702

For each record the package stores Standard InChI, Standard InChIKey, PubChem/OEChem SMILES, molecular formula, formal charge and source-version metadata.

## Checks performed before upload

- 5 unique internal `structure_id` values.
- 5 unique Standard InChIKey values.
- all Standard InChI values use the `InChI=1S/` prefix.
- all InChIKey values match the standard 27-character form.
- sibling references resolve to IDs observed in `packages/organic/data/core_substances.yaml` on the branch baseline.

Local chemistry-toolkit parse/valence/formula/charge validation has **not** yet been run, so the records remain `provisional` and the Substance↔Structure relations remain `candidate`.

Next quality gate: cross-check priority records with ChEBI where available, then run a pinned RDKit validator and promote only records that satisfy package validation rules.
